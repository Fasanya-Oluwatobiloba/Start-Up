import SIMME from "../util/Department.js";
import SESE from "../util/Department.js";
import SAAT from "../util/Department.js";
import SET from "../util/Department.js";
import SEMS from "../util/Department.js";
import SLIT from "../util/Department.js";
import SPS from "../util/Department.js";
import SBMS from "../util/Department.js";
import SLS from "../util/Department.js";
import SOC from "../util/Department.js";

const SIMME = () => {
  return (
    <div className="container mx-auto p-4">
      <ul className="list-none space-y-2">
        {SIMME.map((program, index) => (
          <li
            key={index}
            className="text-xl text-gray-700 font-semibold bg-gray-100 p-2 rounded-md shadow-md"
          >
            {program}
          </li>
        ))}
      </ul>
    </div>
  );
};

export default SIMME;

export const SESE = () => {
	return (
	  <div className="container mx-auto p-4">
		<ul className="list-none space-y-2">
		  {SESE.map((program, index) => (
			<li
			  key={index}
			  className="text-xl text-gray-700 font-semibold bg-gray-100 p-2 rounded-md shadow-md"
			>
			  {program}
			</li>
		  ))}
		</ul>
	  </div>
	);
  };
  
  export const SAAT = () => {
	return (
	  <div className="container mx-auto p-4">
		<ul className="list-none space-y-2">
		  {SAAT.map((program, index) => (
			<li
			  key={index}
			  className="text-xl text-gray-700 font-semibold bg-gray-100 p-2 rounded-md shadow-md"
			>
			  {program}
			</li>
		  ))}
		</ul>
	  </div>
	);
  };
  

  export const SET = () => {
	return (
	  <div className="container mx-auto p-4">
		<ul className="list-none space-y-2">
		  {SET.map((program, index) => (
			<li
			  key={index}
			  className="text-xl text-gray-700 font-semibold bg-gray-100 p-2 rounded-md shadow-md"
			>
			  {program}
			</li>
		  ))}
		</ul>
	  </div>
	);
  };
  

  export const SEMS = () => {
	return (
	  <div className="container mx-auto p-4">
		<ul className="list-none space-y-2">
		  {SEMS.map((program, index) => (
			<li
			  key={index}
			  className="text-xl text-gray-700 font-semibold bg-gray-100 p-2 rounded-md shadow-md"
			>
			  {program}
			</li>
		  ))}
		</ul>
	  </div>
	);
  };
  

  export const SLIT = () => {
	return (
	  <div className="container mx-auto p-4">
		<ul className="list-none space-y-2">
		  {SLIT.map((program, index) => (
			<li
			  key={index}
			  className="text-xl text-gray-700 font-semibold bg-gray-100 p-2 rounded-md shadow-md"
			>
			  {program}
			</li>
		  ))}
		</ul>
	  </div>
	);
  };
  

  export const SPS = () => {
	return (
	  <div className="container mx-auto p-4">
		<ul className="list-none space-y-2">
		  {SPS.map((program, index) => (
			<li
			  key={index}
			  className="text-xl text-gray-700 font-semibold bg-gray-100 p-2 rounded-md shadow-md"
			>
			  {program}
			</li>
		  ))}
		</ul>
	  </div>
	);
  };
  

  export const SBMS = () => {
	return (
	  <div className="container mx-auto p-4">
		<ul className="list-none space-y-2">
		  {SESE.map((program, index) => (
			<li
			  key={index}
			  className="text-xl text-gray-700 font-semibold bg-gray-100 p-2 rounded-md shadow-md"
			>
			  {program}
			</li>
		  ))}
		</ul>
	  </div>
	);
  };
  
  export const SLS = () => {
	return (
	  <div className="container mx-auto p-4">
		<ul className="list-none space-y-2">
		  {SLS.map((program, index) => (
			<li
			  key={index}
			  className="text-xl text-gray-700 font-semibold bg-gray-100 p-2 rounded-md shadow-md"
			>
			  {program}
			</li>
		  ))}
		</ul>
	  </div>
	);
  };

  export const SOC = () => {
	return (
	  <div className="container mx-auto p-4">
		<ul className="list-none space-y-2">
		  {SOC.map((program, index) => (
			<li
			  key={index}
			  className="text-xl text-gray-700 font-semibold bg-gray-100 p-2 rounded-md shadow-md"
			>
			  {program}
			</li>
		  ))}
		</ul>
	  </div>
	);
  };
  
  


  
 